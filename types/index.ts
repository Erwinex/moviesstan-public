import { type IconType } from "react-icons";

export type MenuItemProp = {
  lable: string;
  address: string;
  Icon: IconType;
};

export type HeaderItemProps = {
  lable: string;
  param: string;
};
